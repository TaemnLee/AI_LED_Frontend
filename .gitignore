# Local .terraform directories (provider binaries, external modules)
terraform/.terraform

# Terraform state files (contain sensitive resource attributes)
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Variable files (often contain secrets like passwords or keys)
*.tfvars
*.tfvars.json

# Override files (used for local overrides)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Terraform CLI configuration files
.terraformrc
terraform.rc

# Terraform plan output files (optional, depending on usage)
*.tfplan

# Dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Testing
/coverage

# Next.js build artifacts
/.next/
/out/

# Production build
/build

# Miscellaneous
.DS_Store
*.pem

# Debugging logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Environment variables (optional, depends on use case)
.env*

# Vercel
.vercel

# TypeScript-related
*.tsbuildinfo
next-env.d.ts
